#include "main.h"
/**
 * mul - multiplie two number
 * @a: the first character to multiplie
 * @b: the second character to multiplie
 *
 * Return: the result
 */
int mul(int a, int b)
{
	return (a * b);
}
